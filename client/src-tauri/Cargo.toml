[package]
name = "music-library"
version = "0.1.0"
description = "Music Library desktop client"
edition = "2021"

[lib]
name = "music_library_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }